# ============================================================================
# Frappe 15.121.0 on PostgreSQL -- PyPI dependency set
#
# 147 packages, exact pinned versions, taken from a working install that serves
# the Desk UI. Every one of these resolves from PyPI (or your internal mirror).
#
# TEST AVAILABILITY WITHOUT INSTALLING ANYTHING:
#     pip download -r requirements.txt -d wheelhouse
# or, for a Windows target from any machine:
#     pip download --no-deps -r requirements.txt -d wheelhouse \
#         --platform win_amd64 --python-version 3.11 --only-binary=:all:
#     (--no-deps matters: this file is already a complete flattened set, and
#      without it pip chases transitive deps and fails on the sdist-only ones.)
# or run the reporting script, which also checks the non-PyPI pieces:
#     python scripts/check_availability.py
#
# NOT IN THIS FILE -- these cannot come from PyPI. See requirements-github.txt.
#   frappe        the framework itself; PyPI has only a 0.0.1 placeholder
#   PyPika        PyPI's PyPika 0.48.9 is a DIFFERENT library from frappe's fork
#                 of the same version number (~850 lines differ in the modules
#                 frappe uses most). Installing PyPI's version fails silently.
#   gunicorn      not required at all -- zero `import gunicorn` in frappe;
#                 winbench uses waitress instead. Listed only so you know we
#                 deliberately dropped it.
#
# Windows notes (all 150 checked against PyPI):
#   126 pure-Python wheels, 17 have win_amd64 wheels, 7 are sdist-only:
#     cairocffi, docopt, maxminddb-geolite2, PyQRCode, rauth,
#     traceback-with-variables, zxcvbn
#   All 7 sdists were inspected for C extensions -- all are pure Python, so pip
#   builds them locally. NO PACKAGE IN THIS FILE NEEDS A C COMPILER ON WINDOWS.
#   (rauth publishes only a py2-none-any wheel, which pip correctly rejects for
#   3.11, so it resolves as an sdist.)
#   cairocffi loads the cairo/GTK3 DLLs at *runtime*, and only if you use
#   WeasyPrint for PDFs. Skip WeasyPrint and it is never loaded.
# ============================================================================

annotated-types==0.8.0
asttokens==3.0.2
Babel==2.13.1
backcall==0.2.0
backoff==2.2.1
beautifulsoup4==4.12.3
bleach-allowlist==1.0.3
bleach==6.4.0
boto3==1.34.162
botocore==1.34.162
brotli==1.2.0
cachetools==7.2.0
cairocffi==1.5.1
certifi==2026.7.22
cffi==2.1.1
chardet==5.2.0
charset-normalizer==3.5.1
click==8.4.2
croniter==6.0.0
cryptography==50.0.1
cssselect2==0.10.1
cssselect==1.5.0
cssutils==2.11.1
decorator==5.3.1
distro==1.9.0
docopt==0.6.2
dropbox==12.0.2
duckdb==1.4.5
email-reply-parser==0.5.12
et_xmlfile==2.0.0
executing==2.2.1
filelock==3.20.4
filetype==1.2.0
flit_core==4.1.0
fonttools==4.65.0
gitdb==4.0.12
GitPython==3.1.62
google-api-core==2.37.0
google-api-python-client==2.188.0
google-auth-httplib2==0.4.2
google-auth-oauthlib==1.2.4
google-auth==2.48.0
googleapis-common-protos==1.75.3
hiredis==2.2.3
html5lib==1.1
httplib2==0.32.0
idna==3.19
iniconfig==2.3.0
ipython==8.15.0
jedi==0.20.0
Jinja2==3.1.6
jmespath==1.1.0
ldap3==2.9.1
lxml==6.1.3
markdown2==2.5.5
markdownify==0.14.1
MarkupSafe==3.0.3
matplotlib-inline==0.2.2
# maxminddb-geolite2 and maxminddb are deliberately NOT here.
#
# The framework uses them only to guess a country from an IP address, in the
# setup wizard and on a session record. It wraps the import in try/except
# ImportError and returns None when they are absent, so nothing breaks.
#
# They are removed because of the data, not the code. The Python wrapper is
# BSD-licensed, but the package embeds a 2018 snapshot of a geo-IP database
# whose upstream terms changed the following year. Explaining that to a
# licence-review team, for a feature this product does not use, costs more than
# the feature is worth. 724 tests pass without them.
monotonic==1.6
more-itertools==11.1.0
num2words==0.5.14
oauthlib==3.3.1
openpyxl==3.1.5
opentelemetry-api==1.44.0
packaging==26.3
parso==0.8.7
passlib==1.7.4
pdfkit==1.0.0
pexpect==4.9.0
phonenumbers==9.0.39
pickleshare==0.7.5
pillow==12.3.0
pip==26.2.1
pluggy==1.6.0
ply==3.11
posthog==3.21.0
premailer==3.10.0
prompt_toolkit==3.0.53
proto-plus==1.28.4
protobuf==7.36.1
psutil==5.9.8
psycopg2-binary==2.9.13
ptyprocess==0.7.0
pure_eval==0.2.4
pyarrow==25.0.1
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.12.5
pydantic_core==2.41.5
pydyf==0.12.1
Pygments==2.21.0
PyJWT==2.13.0
PyMySQL==1.1.1
pyOpenSSL==26.4.0
pyotp==2.9.0
pyparsing==3.3.2
pypdf==6.15.0
pyphen==0.18.1
PyQRCode==1.2.1
pytest==9.1.1
python-dateutil==2.9.0.post0
pytz==2025.2
PyYAML==6.0.3
rauth==0.7.3
redis==4.5.5
requests-oauthlib==1.3.1
requests==2.33.1
RestrictedPython==8.5
rq==1.15.1
rsa==4.9.1
s3transfer==0.10.4
semantic-version==2.10.0
sentry-sdk==1.45.1
setuptools==84.0.0
six==1.17.0
smmap==5.0.3
soupsieve==2.9.2
sqlglot==30.18.0
sqlparse==0.6.0
sql_metadata==3.0.1
stack-data==0.6.3
stone==3.3.1
tenacity==8.2.3
terminaltables==3.1.10
tinycss2==1.5.1
tinyhtml5==2.1.0
tomli==2.0.2
traceback-with-variables==2.0.4
traitlets==5.16.1
typing-inspection==0.4.4
typing_extensions==4.16.0
uritemplate==4.2.0
urllib3==2.8.0
vobject==0.9.9
waitress==3.0.2
wcwidth==0.8.3
weasyprint==69.0
webencodings==0.6.1
Werkzeug==3.1.6
wheel==0.48.0
Whoosh==2.7.4
xlrd==2.0.2
zopfli==0.4.3
zxcvbn==4.4.28
