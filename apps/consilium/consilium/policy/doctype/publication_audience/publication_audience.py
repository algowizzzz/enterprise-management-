"""Publication Audience — controller.

One audience a publication targeted (P-19).
"""

from frappe.model.document import Document


class PublicationAudience(Document):
    def validate(self):
        pass
