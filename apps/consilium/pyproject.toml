[project]
name = "consilium"
authors = [{ name = "Consilium", email = "dev@example.com" }]
description = "Enterprise Governance, Risk & Policy platform"
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "consilium"
